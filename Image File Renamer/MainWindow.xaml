<Window
    x:Class="PhotoOrganizer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Image File Renamer"
    Height="450" Width="600"
    Background="#1E1E1E" Foreground="White">

    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <!-- Source folder -->
            <Button x:Name="SourceButton" Content="Select Source Folder" Width="250" Margin="5"
                    Background="#2D2D30" Foreground="White" Click="SourceButton_Click"/>
            <TextBlock x:Name="SourcePathText" Text="No source selected" Margin="5"
                       Foreground="LightGray" TextAlignment="Center"/>

            <!-- Target folder -->
            <Button x:Name="TargetButton" Content="Select Target Folder" Width="250" Margin="5"
                    Background="#2D2D30" Foreground="White" Click="TargetButton_Click"/>
            <TextBlock x:Name="TargetPathText" Text="No target selected" Margin="5"
                       Foreground="LightGray" TextAlignment="Center"/>

            <!-- Duplicate handling dropdown -->
            <StackPanel Margin="10" Orientation="Vertical">
                <TextBlock Text="Duplicate Handling:" Foreground="White" Margin="5"/>
    
                <RadioButton x:Name="AppendCounterOption" Content="Append Counter" IsChecked="True"
                             Foreground="White" Background="#2D2D30" Margin="5"/>
    
                <RadioButton x:Name="SkipDuplicateOption" Content="Skip Duplicate"
                             Foreground="White" Background="#2D2D30" Margin="5"/>
    
                <RadioButton x:Name="OverwriteOption" Content="Overwrite Existing"
                             Foreground="White" Background="#2D2D30" Margin="5"/>
            </StackPanel>

            <Button x:Name="OrganizeButton" Content="Organize Photos" Width="250" Margin="5"
                    Background="#007ACC" Foreground="White" Click="OrganizeButton_Click"/>

            <!-- Progress bar + counter -->
            <ProgressBar x:Name="ProgressBar" Width="400" Height="20" Margin="10" Minimum="0" Maximum="100"/>
            <TextBlock x:Name="ProgressText" Text="Progress: 0/0" Margin="5"
                       TextAlignment="Center" Foreground="LightGray"/>

            <TextBlock x:Name="StatusText" Text="Ready" Margin="10"
                       TextAlignment="Center" Foreground="LightGray"/>
        </StackPanel>
    </Grid>
</Window>