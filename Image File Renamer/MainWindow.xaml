<Window x:Class="ImageFileRenamer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Image File Renamer" Height="300" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Folder Selection -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBox x:Name="FolderPathBox" Width="350" Margin="0,0,10,0"/>
            <Button x:Name="BrowseButton" Content="Browse..." Width="100" Click="BrowseButton_Click"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Button x:Name="RenameButton" Content="Rename Files" Width="120" Click="RenameButton_Click"/>
        </StackPanel>

        <!-- Status Output -->
        <TextBlock x:Name="StatusText" Grid.Row="2" Text="Ready." 
                   VerticalAlignment="Top" Foreground="Gray"/>
    </Grid>
</Window>